#ifndef FUNCTIONS_H
#define FUNCTIONS_H
#include <QString>
#include <cctype>
#include <QChar>
bool isValidDat(const QString& d);
bool isAlpha(const QString& ch);
bool isDigital(const QString& c);
#endif // FUNCTIONS_H
